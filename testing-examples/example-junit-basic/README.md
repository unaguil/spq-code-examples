Basic JUnit + JaCoCo Example
============================

To launch the JUnit tests use:

    mvn test

Launch tests verifying that they match the coverage requirements:

    mvn verify

Execute reporting plugins and generate project site:

    mvn site
