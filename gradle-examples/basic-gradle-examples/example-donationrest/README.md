Donations REST Example
----------------------

First, the whole code can be compiled using:

    ./gradlew compileJava

Then, in three separate shell windows, run the following commands:

    ./gradlew appRun
    ./gradlew runManager
    ./gradlew runClient
